#include<stdio.h>
int main(){
int number, divisor;

printf("number: ");
scanf("%d", &number);

    if(number%7==0)
        printf("%d is divisible by 7.", number);
    else
        printf("%d is not divisible by 7.", number);

        }
